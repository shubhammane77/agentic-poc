# TODO

1. Prompt evolution with offline optimizer
- Use existing trace/evaluation artifacts to run DSPy optimization per repo archetype.
- Pin and version prompt bundles after benchmarked improvements.

2. Human review packet per commit
- Auto-generate a compact review note per proposed commit:
  changed test file, selection rationale, behavior covered, risk notes, and file-level coverage delta.
